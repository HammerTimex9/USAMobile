import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
	container:{},
	bodyWrapper:{ paddingHorizontal:8},
	labelWrapper:{ marginVertical: 10, alignItems:'center'},
	fromSelectWrapper:{}
});

export default styles;
